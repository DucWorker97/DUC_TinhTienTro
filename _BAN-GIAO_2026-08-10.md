# BÀN GIAO — DUC_TinhTienTro (Rent Calculator / Máy Tính Tiền Trọ)

- **Ngày tạo:** 10/08/2026 (Thứ Hai)
- **Người thực hiện:** AI agent (phiên làm việc với Nguyễn Duy Đức)
- **Loại:** Web app single-file, client-side
- **File chính:** `artifacts/DUC_TinhTienTro.html`
- **File phụ:** `artifacts/GiaoDien-TienTro.svg` (minh họa giao diện, số liệu mẫu)

---

## A. VIỆC ĐÃ LÀM

### A1. Kiến trúc & tech stack

- Single-file `DUC_TinhTienTro.html` — không backend, không database, chạy 100% client-side.
- HTML5 + Vanilla JavaScript + Tailwind CSS (CDN).
- Thêm 1 thư viện CDN: `html2canvas@1.4.1` (phục vụ tải ảnh hóa đơn).

### A2. Cấu trúc UI (mobile-first, `max-w-md` căn giữa)

- **Section 1 — Form nhập:** thẻ trắng, header thanh xanh `blue-600`, gom input thành card con:- Kỳ thanh toán (`input type="month"`)
- Đơn giá (điện `4000`, nước `13000`)
- Chi phí cố định (phòng `2700000`, rác `20000`, phụ thu `0`)
- Chỉ số công tơ (điện/nước cũ–mới)
- 2 người: tên (mặc định "Đức"/"Thảo", **sửa được**), điện riêng (step 0.1), số ngày ở (`30`), trả trước (`0`)
- **Section 2 — Hóa đơn:** ẩn mặc định, hiện sau khi bấm "XUẤT HÓA ĐƠN".- Header gradient xanh + tháng cỡ `3xl`
- Summary: tổng điện, tổng nước, tổng chi phí chung
- 2 card người: breakdown `[chung] + [điện riêng] − [trả trước]`, badge % chung, dòng **PHẢI TRẢ** cỡ `3xl` (Người 1 xanh dương, Người 2 xanh cyan)

### A3. Logic tính toán (đã kiểm thử bằng bộ số mẫu)

```
Tổng nước       = nước mới − nước cũ;   Tiền nước = tổng nước × đơn giá nước
Tổng điện       = điện mới − điện cũ
Điện chung      = tổng điện − (điện riêng Đức + điện riêng Thảo)
Tổng chi phí chung = phòng + rác + tiền nước + phụ thu + (điện chung × đơn giá điện)
Tỉ lệ (mỗi người)  = số ngày ở / tổng số ngày ở  (chia 0 → 0.5/người)
Phải trả (mỗi người) = (tổng chi phí chung × tỉ lệ) + (điện riêng × đơn giá điện) − trả trước

```

- **Đã nghiệm thu logic:** bộ số mẫu (200 kWh, 10 m³, phòng 2.7tr...) → tổng 2 người = 3.150.000đ, khớp chính xác với `chung + điện riêng − trả trước`.

### A4. Tính năng bổ sung

- **Định dạng tiền VND:** `toLocaleString('vi-VN')`, làm tròn số nguyên, hậu tố `đ` (vd `1.500.000 đ`).
- **Tự lưu localStorage** (`STORAGE_KEY = 'tienTro_v1'`): mọi input tự lưu khi gõ, mở lại khôi phục.
- **Nút Đặt lại:** xóa localStorage + reload (có xác nhận).
- **Tải ảnh hóa đơn:** `html2canvas` scale 2, tên file `HoaDon_TienTro_MM-YYYY.png`.
- **Sửa tên người:** tên hiển thị động lên hóa đơn; đã escape HTML (chống lỗi/XSS); bỏ trống → fallback "Người 1/2".

### A5. Thiết kế

- Phong cách **fintech xanh dương**; label form đồng bộ `blue-700`.
- File SVG minh họa giao diện (vector, tiếng Việt có dấu đầy đủ).

---

## B. RỦI RO / TECHNICAL DEBT / ĐIỂM CẦN LƯU Ý

| # | Mức | Vấn đề | Chi tiết & hướng xử lý |
| --- | --- | --- | --- |
| B1 | 🔴 | **Chưa nghiệm thu trên trình duyệt/mobile thật** | Mới xem trong session tab. Chưa bấm thử "XUẤT HÓA ĐƠN" và "Tải ảnh hóa đơn" trên Chrome/điện thoại. **Cần test thủ công trước khi dùng thật.** |
| B2 | 🟠 | **Phụ thuộc mạng (CDN)** | Tailwind + html2canvas load từ CDN → **không có mạng là app hỏng giao diện + không tải được ảnh**. Muốn offline phải nhúng inline hoặc build Tailwind cục bộ. |
| B3 | 🟠 | **html2canvas trên mobile** | Một số trình duyệt di động mở ảnh ở tab mới thay vì tải thẳng; emoji có thể render khác nhau giữa thiết bị. |
| B4 | 🟡 | **Cố định đúng 2 người** | Tên "Đức/Thảo" sửa được nhưng **số người cứng = 2**. Muốn 3+ người phải refactor sang mảng động. |
| B5 | 🟡 | **Không validate input** | Nhập điện mới < điện cũ → ra số âm mà không cảnh báo. Chưa chặn giá trị vô lý. |
| B6 | 🟡 | **localStorage theo trình duyệt** | Dữ liệu chỉ lưu trên đúng máy + trình duyệt đó; xóa cache là mất. Không đồng bộ nhiều thiết bị. |
| B7 | ⚪ | **SVG là minh họa tĩnh** | `GiaoDien-TienTro.svg` dùng số liệu mẫu, bố cục mô phỏng gần đúng — **không phải bản render thật** của HTML. |
| B8 | ⚪ | **Tailwind CDN cảnh báo production** | Tailwind khuyến cáo không dùng CDN cho production; chấp nhận được với app cá nhân/nội bộ. |

---

## C. GỢI Ý VIỆC TIẾP THEO (chưa làm)

- Nút "Chia sẻ" trực tiếp qua Web Share API (Zalo/Messenger).
- Thêm dòng ngày giờ xuất hóa đơn vào receipt.
- Validate input (chặn số âm, chỉ số mới ≥ cũ).
- Hỗ trợ N người (refactor động).
- Nhúng Tailwind/html2canvas inline để chạy offline hoàn toàn.

---

## D. CÁCH CHẠY / KIỂM THỬ

1. Mở `DUC_TinhTienTro.html` bằng Chrome (double-click hoặc kéo vào trình duyệt) — cần có mạng cho CDN.
2. Nhập số liệu → bấm **XUẤT HÓA ĐƠN** → kiểm tra breakdown + PHẢI TRẢ.
3. Bấm **📸 Tải ảnh hóa đơn** → xác nhận file PNG xuất ra đúng, chữ nét.
4. Reload trang → xác nhận số liệu vẫn còn (localStorage).
5. Chụp màn hình trên điện thoại → kiểm tra độ tương phản khi chia sẻ.

> ⚠️ Trạng thái chung: **logic đã nghiệm thu bằng số mẫu; UI/tải ảnh CHƯA nghiệm thu trên trình duyệt thật.**

